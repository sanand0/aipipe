{
  "name": "aipipe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "tap run",
    "lint": "npx -y prettier@3.5 --print-width 120 '**/*.{js,md}' --write && npx -y oxlint --fix",
    "prepublishOnly": "npm run lint"
  },
  "prettier": {
    "printWidth": 120
  },
  "type": "module",
  "devDependencies": {
    "tap": "^21.1.0",
    "wrangler": "^4.12.0",
    "jsdom": "^24.0.0"
  },
  "dependencies": {
    "jose": "^6.0.10"
  }
}
